export enum Roles {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum RoleNames {
  ADMIN = 'Администратор',
  USER = 'Участник',
}

export enum RoleDescriptions {
  ADMIN = 'Может менять настройки рабочего пространства и добавлять новых участников в проект',
  USER = 'Не может менять настройки рабочего пространства или добавлять новых участников в проект',
}